# Local display font

Place a properly licensed Miloner Medium webfont in this directory only after
obtaining it from an authorized source. Until then, the display font token uses
the safe Georgia fallback declared in `src/app/globals.css`.
